#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	//int x, y;
	//for (x = 0, y = 0; x < 2 && y < 5; ++x, ++y)
	//{
	//	printf("hehe\n");
	//}
	int i = 0;
	int k = 0;
	for (i = 0, k = 0; k = 0; i++, k++)
	{
		k++;
		printf("%d\n", k);
	}
	return 0;
}